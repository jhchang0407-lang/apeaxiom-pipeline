# Railway config — service-specific overrides are set in Railway UI per service.
# market-data-cron: Dockerfile.market_data, python run_market_data.py, cron 0 10 * * *
# apeaxiom-pipeline: Dockerfile (default), python run_master.py
